# Archivo .gitignore para el proyecto 'inference-engine'

# ------------------------------------
# 1. Dependencias de Node.js
# ------------------------------------
/node_modules
/npm-debug.log*
/yarn-debug.log*
/yarn-error.log*
.pnp
.pnp.js
.pnpm-debug.log*

# ------------------------------------
# 2. Archivos de compilación y temporales
# ------------------------------------
/dist
/build
/tmp

# ------------------------------------
# 3. Entorno y Configuración Local
# ------------------------------------
# El archivo .env contiene secretos o configuración local
.env
.env.local
.env.development
.env.test

# ------------------------------------
# 4. Archivos de Modelo y Caché
# ------------------------------------
# Los modelos .onnx pueden ser archivos muy grandes.
# Es mejor excluirlos y usar Git LFS si necesitas rastrearlos,
# o excluirlos completamente y descargarlos al hacer el setup.
# En este caso, excluimos todo el directorio 'models/'
# excepto si Git LFS ya está en uso, en cuyo caso, esta línea ignora si no está configurado.
/models/

# Si decides usar Git LFS, puedes comentar la línea anterior, pero
# aseguramos que el archivo de modelo real no se suba si es gigante:
# *.onnx

# ------------------------------------
# 5. Datos de Prueba y Cargas de Inferencia (Payloads)
# ------------------------------------
# Los archivos de payload de inferencia contienen datos codificados (Base64) que son variables y grandes.
# Los archivos de imágenes de prueba pueden ser grandes y no son código.
/data/test_images/
inference_payload.json

# ------------------------------------
# 6. Archivos del Sistema Operativo
# ------------------------------------
# macOS
.DS_Store

# Windows
Thumbs.db
ehthumbs.db